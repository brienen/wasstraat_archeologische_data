[pytest]
testpaths = tests
pythonpath = airflow_app/dags .
markers =
    unit: Pure unit tests zonder externe dependencies
    integration: Integratietests met synthetische data (MongoDB/Airflow)
    delft: Integratietests met Delftse testdata (DB034): Extract + Transform
    delft_load: Delft Load-tests: MongoDB naar PostgreSQL
    delft_flask: Delft Flask smoke tests
    flask_smoke: Flask smoke tests (Docker: PostgreSQL + Redis + Flask)
    load: Load-tests: MongoDB naar PostgreSQL (vereist volledige pipeline)
addopts = -v --tb=short
filterwarnings =
    ignore::DeprecationWarning:flask_sqlalchemy
    ignore::DeprecationWarning:flask_appbuilder
    ignore::DeprecationWarning:flask_babel
    ignore:.*MovedIn20Warning.*:sqlalchemy.exc.MovedIn20Warning
    ignore::DeprecationWarning:marshmallow_sqlalchemy
