[pytest]
testpaths = tests
pythonpath = airflow_app/dags .
markers =
    unit: Pure unit tests zonder externe dependencies
    integration: Integratietests die MongoDB/PostgreSQL nodig hebben
addopts = -v --tb=short
